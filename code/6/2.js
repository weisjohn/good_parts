// length

// an array's .length property is not the same as the number of elements in the array
console.log('var arr = [ 1,,,,,2 ];')
var arr = [ 1,,,,,2 ];
console.log(arr);
console.log('arr.length', arr.length);

// great source for a trick question